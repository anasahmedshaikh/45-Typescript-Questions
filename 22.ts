let colors: string[] = ['red', 'green', 'blue'];

// Try to access an element with an index that is out of bounds
console.log(colors[3]); // This will produce an index error

// After noticing the error, let's correct it
console.log(colors[2]); // Correct index to access the third element
