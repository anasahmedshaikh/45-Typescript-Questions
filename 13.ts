// Task 13

let favtransport : string [] = ["airplane", "car", "public bus", "ferrari",];
favtransport.map((items) => console.log(`I like to travel in ${items}.`));
console.log(`My favourate mode of transportation is ${favtransport[0]} because it is the fastest.`);