// Function to make a shirt
function makeT_shirt (size: string, message: string): void {
    console.log(`Shirt size: ${size.toUpperCase()}, This will be printed on your t-shirt ${message}`);
}

// Call the function
make_shirt("medium", "call of duty black oops");
