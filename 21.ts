// Define car objects
let car1: { brand: string, model: string, year: number } = {
    brand: 'Toyota',
    model: 'Camry',
    year: 2020
};

let car2: { brand: string, model: string, year: number } = {
    brand: 'Honda',
    model: 'Civic',
    year: 2018
};

let car3: { brand: string, model: string, year: number } = {
    brand: 'Ford',
    model: 'Mustang',
    year: 2019
};

// Print car information
console.log("Car 1:", car1);
console.log("Car 2:", car2);
console.log("Car 3:", car3);
